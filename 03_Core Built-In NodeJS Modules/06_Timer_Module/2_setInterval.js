/** 
 * @description
 *!  setInterval()
 *   setInterval allows us to schedule a function to run repeatedly at regular intervals.
*/
setInterval(() => {
    console.log('Timer Callback (Event Queue) - This runs every 3 seconds');
}, 3000);

// Output: Timer Callback (Event Queue) - This runs every 3 seconds


