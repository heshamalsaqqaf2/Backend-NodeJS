/** 
 * @description
 *!  setTimeout()
 *   setTimeout is used to schedule a function to be executed once after
 *   a specified number of milliseconds.
*/

setTimeout(() => {
    console.log('Timer Callback (Event Queue) - This runs after 2 seconds');
}, 2000);